import 'package:flutter/material.dart';

import 'module_root.dart' as scroll_table;

class ScrollTableEntry extends StatelessWidget {
  const ScrollTableEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const scroll_table.ScrollTableDemo();
  }
}
